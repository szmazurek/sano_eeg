"""Legacy - used only for preliminary experiments during the
pre-processing experiments, not used in any further work.
"""
import utils
from pathlib import Path

utils.create_recordings_plv_old(Path("data/temp_folder"), Path("data/plv_arrays"))
